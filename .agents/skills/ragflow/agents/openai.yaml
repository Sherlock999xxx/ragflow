interface:
  display_name: "Ragflow"
  short_description: "Repo-specific patterns and workflows for ragflow"
  default_prompt: "Use the ragflow repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true